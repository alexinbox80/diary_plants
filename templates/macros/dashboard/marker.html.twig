{% macro marker(color, letter) %}
{#  todo: размер маркеров скачет как угодно нужно привести к одному виду как в дневнике календаря #}
    <span style="background-color: {{ color }}; border: 1px solid; padding: 2px; align-content: center; align-items: center;">{{ letter }}</span>
{% endmacro %}
