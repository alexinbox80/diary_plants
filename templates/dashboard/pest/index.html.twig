{% extends 'layout/dashboard.html.twig' %}

{% block header %}
    {% include 'components/dashboard/navbar.html.twig' with { 'title': 'dashboard.pest.title'|trans } %}
{% endblock %}

{% block body %}
    <div class="container-fluid page-body-wrapper">
        {% include 'components/dashboard/sidebar.html.twig' %}
        <div class="main-panel">
            <div class="content-wrapper">
                {% if pagination is defined %}
                    {% include 'components/dashboard/table.html.twig' with
                        {
                            'title': 'dashboard.pest.list'|trans,
                            'array': pests,
                            'pagination': pagination,
                            'route_index': 'dashboard.pests.index',
                            'route_pagination': 'dashboard.pests_paginated.index',
                            'route_edit': 'dashboard.pests.edit',
                            'route_create': 'dashboard.pests.create',
                            'route_delete': 'dashboard.pests.delete'
                        }
                    %}
                {% else %}
                    {% include 'components/dashboard/table.html.twig' with
                        {
                            'title': 'dashboard.pest.list'|trans,
                            'array': pests,
                            'route_index': 'dashboard.pests.index',
                            'route_pagination': 'dashboard.pests_paginated.index',
                            'route_edit': 'dashboard.pests.edit',
                            'route_create': 'dashboard.pests.create',
                            'route_delete': 'dashboard.pests.delete'
                        }
                    %}
                {% endif %}
            </div>
            <!-- content-wrapper ends -->
            {% include 'components/dashboard/footer.html.twig' %}
        </div>
        <!-- main-panel ends -->
    </div>
{% endblock %}

{% block javascripts %}
    {{ parent() }}
    <script type="module" defer src="{{ asset('assets/js/dashboard/table/main.js') }}" data-entity="pest"></script>
{% endblock %}
