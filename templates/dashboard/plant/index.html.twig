{% extends 'layout/dashboard.html.twig' %}

{% block header %}
    {% include 'components/dashboard/navbar.html.twig' with { 'title': 'dashboard.plant.title'|trans } %}
{% endblock %}

{% block body %}
    <div class="container-fluid page-body-wrapper">
        {% include 'components/dashboard/sidebar.html.twig' %}
        <div class="main-panel">
            <div class="content-wrapper">
                {% if pagination is defined %}
                    {% include 'components/dashboard/table.html.twig' with
                        {
                            'title': 'dashboard.plant.list'|trans,
                            'array': plants,
                            'pagination': pagination,
                            'route_index': 'dashboard.plants.index',
                            'route_pagination': 'dashboard.plants_paginated.index',
                            'route_edit': 'dashboard.plants.edit',
                            'route_create': 'dashboard.plants.create',
                            'route_delete': 'dashboard.plants.delete'
                        }
                    %}
                {% else %}
                    {% include 'components/dashboard/table.html.twig' with
                        {
                            'title': 'dashboard.plant.list'|trans,
                            'array': plants,
                            'route_index': 'dashboard.plants.index',
                            'route_pagination': 'dashboard.plants_paginated.index',
                            'route_edit': 'dashboard.plants.edit',
                            'route_create': 'dashboard.plants.create',
                            'route_delete': 'dashboard.plants.delete'
                        }
                    %}
                {% endif %}
            </div>
            <!-- content-wrapper ends -->
            {% include 'components/dashboard/footer.html.twig' %}
        </div>
        <!-- main-panel ends -->
    </div>
{% endblock %}

{% block javascripts %}
    {{ parent() }}
    <script type="module" defer src="{{ asset('assets/js/dashboard/table/main.js') }}" data-entity="plant"></script>
{% endblock %}
