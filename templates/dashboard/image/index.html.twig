{% extends 'layout/dashboard.html.twig' %}

{% block header %}
    {% include 'components/dashboard/navbar.html.twig' with { 'title': 'dashboard.image.title'|trans } %}
{% endblock %}

{% block body %}
    <div class="container-fluid page-body-wrapper">
        {% include 'components/dashboard/sidebar.html.twig' %}
        <div class="main-panel">
            <div class="content-wrapper">
                {% if pagination is defined %}
                    {% include 'components/dashboard/table.html.twig' with
                        {
                            'title': 'dashboard.image.list'|trans,
                            'array': images,
                            'pagination': pagination,
                            'route_index': 'dashboard.images.index',
                            'route_pagination': 'dashboard.images_paginated.index',
                            'route_edit': 'dashboard.images.edit',
                            'route_create': 'dashboard.images.create',
                            'route_delete': 'dashboard.images.delete'
                        }
                    %}
                {% else %}
                    {% include 'components/dashboard/table.html.twig' with
                        {
                            'title': 'dashboard.image.list'|trans,
                            'array': images,
                            'route_index': 'dashboard.images.index',
                            'route_pagination': 'dashboard.images_paginated.index',
                            'route_edit': 'dashboard.images.edit',
                            'route_create': 'dashboard.images.create',
                            'route_delete': 'dashboard.images.delete'
                        }
                    %}
                {% endif %}
            </div>
            <!-- content-wrapper ends -->
            {% include 'components/dashboard/footer.html.twig' %}
        </div>
        <!-- main-panel ends -->
    </div>
{% endblock %}

{% block javascripts %}
    {{ parent() }}
    <script type="module" defer src="{{ asset('assets/js/dashboard/table/main.js') }}" data-entity="image"></script>
{% endblock %}
