{% extends 'layout/dashboard.html.twig' %}

{% block header %}
    {% include 'components/dashboard/navbar.html.twig' with {'title': 'Пользватели'} %}
{% endblock %}

{% block body %}
    <div class="container-fluid page-body-wrapper">
        {% include 'components/dashboard/sidebar.html.twig' %}
        <div class="main-panel">
            <div class="content-wrapper">
                {% if pagination is defined %}
                    {% include 'components/dashboard/table.html.twig' with
                        {
                            'title': 'Список пользователей',
                            'array': users,
                            'pagination': pagination,
                            'route_index': 'dashboard.users.index',
                            'route_pagination': 'dashboard.users_paginated.index',
                            'route_edit': 'dashboard.users.edit',
                            'route_create': 'dashboard.users.create',
                            'route_delete': 'dashboard.users.delete'
                        }
                    %}
                {% else %}
                    {% include 'components/dashboard/table.html.twig' with
                        {
                            'title': 'Список пользователей',
                            'array': users,
                            'route_index': 'dashboard.users.index',
                            'route_pagination': 'dashboard.users_paginated.index',
                            'route_edit': 'dashboard.users.edit',
                            'route_create': 'dashboard.users.create',
                            'route_delete': 'dashboard.users.delete'
                        }
                    %}
                {% endif %}
            </div>
            <!-- content-wrapper ends -->
            {% include 'components/dashboard/footer.html.twig' %}
        </div>
        <!-- main-panel ends -->
    </div>
{% endblock %}

{% block javascripts %}
    {{ parent() }}
    <script type="module" defer src="{{ asset('assets/js/dashboard/table/main.js') }}" data-entity="user"></script>
{% endblock %}
